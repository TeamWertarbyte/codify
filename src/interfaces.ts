export interface Options {
  backgroundColor: string;
  fontColor: string;
  showLineNumbers: boolean;
  lightMode: boolean;
  os: "macOS" | "windows10" | "linuxMint";
  language: string;
  fontFamily: string;
}
